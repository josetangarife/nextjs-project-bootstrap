import type { Metadata } from "next";
import { Geist, Geist_Mono } from "next/font/google";
import "./globals.css";

const geistSans = Geist({
  variable: "--font-geist-sans",
  subsets: ["latin"],
});

const geistMono = Geist_Mono({
  variable: "--font-geist-mono",
  subsets: ["latin"],
});

export const metadata: Metadata = {
  title: "DevSoft - Desarrollo de Software Profesional",
  description: "Empresa líder en desarrollo de software, aplicaciones web y móviles. Ofrecemos soluciones tecnológicas innovadoras para tu negocio.",
  keywords: "desarrollo software, aplicaciones web, aplicaciones móviles, consultoría TI, programación",
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="es">
      <body
        className={`${geistSans.variable} ${geistMono.variable} antialiased`}
      >
        {children}
      </body>
    </html>
  );
}
